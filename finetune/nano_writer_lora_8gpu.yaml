# LoRA SFT — Nemotron-3-Nano-30B-A3B as the inspection-brief writer, 8 GPUs.
#
# NOT runnable under the current omc-team14 QOS, which caps the whole team at a
# single GPU (see CLUSTER_NOTES.md). Use nano_writer_lora_1gpu.yaml here. This
# file is kept because it is the right shape on a cluster that gives you a node,
# and because the two side by side make the single-GPU adaptations explicit.
#
# Derived from NVIDIA's own recipe for this model:
#   examples/llm_finetune/nemotron/nemotron_nano_v3_hellaswag_peft.yaml
# with the dataset swapped for our curated corpus.
#
# Two settings are NOT preferences:
#
#   exclude_modules: ["*.out_proj"]
#     Nemotron-3-Nano is a hybrid Mamba-2 / Transformer MoE. The Mamba-2 layers
#     consume out_proj.weight inside a custom kernel where LoRA cannot apply.
#
#   mask_generation_prompt: true
#     The chat template injects an empty reasoning block into every assistant
#     turn that carries no reasoning_content. Training with loss on that block
#     teaches the model to open its reasoning and immediately close it — and the
#     failure this fine-tune exists to fix is a degenerate reasoning loop.
#
# Paths marked  <<SET>>  are filled in by sbatch_sft_8gpu.sh from $DVIT_DATA.
#
# Run:
#   DVIT_GPUS_PER_NODE=8 sbatch finetune/sbatch_sft_8gpu.sh
# or interactively, inside the container:
#   automodel finetune/nano_writer_lora_8gpu.yaml --nproc-per-node 8

recipe: TrainFinetuneRecipeForNextTokenPrediction

step_scheduler:
  global_batch_size: 16
  local_batch_size: 1
  ckpt_every_steps: 100
  val_every_steps: 50
  max_steps: 400
  num_epochs: 3

dist_env:
  backend: nccl
  # The first run merges split MoE expert tensors on CPU and may pull ~60 GB;
  # give the rendezvous room before it decides a rank is dead.
  timeout_minutes: 90

rng:
  _target_: nemo_automodel.components.training.rng.StatefulRNG
  seed: 1111
  ranked: true

model:
  _target_: nemo_automodel.NeMoAutoModelForCausalLM.from_pretrained
  pretrained_model_name_or_path: nvidia/NVIDIA-Nemotron-3-Nano-30B-A3B-BF16
  torch_dtype: auto
  trust_remote_code: true
  attn_implementation: sdpa
  backend:
    _target_: nemo_automodel.components.models.common.BackendConfig
    rms_norm: torch_fp32
    linear: torch

compile:
  enabled: false

checkpoint:
  enabled: true
  checkpoint_dir: <<SET>>/checkpoints/nano_writer_lora_8gpu
  model_save_format: safetensors
  save_consolidated: final

peft:
  _target_: nemo_automodel.components._peft.lora.PeftConfig
  exclude_modules: ["*.out_proj"]   # Mamba-2 custom kernel; LoRA cannot apply
  dim: 16
  alpha: 32
  use_triton: True

distributed:
  strategy: fsdp2
  dp_size: none
  tp_size: 1
  cp_size: 1
  ep_size: 4
  sequence_parallel: false

loss_fn:
  _target_: nemo_automodel.components.loss.masked_ce.MaskedCrossEntropy

dataset:
  _target_: nemo_automodel.components.datasets.llm.ChatDataset
  path_or_dataset_id: <<SET>>/sft/train.jsonl
  seq_length: 16384
  padding: do_not_pad
  truncation: do_not_truncate
  mask_generation_prompt: true
  skip_invalid_samples: false   # a malformed line is a bug in curate.py, not noise

packed_sequence:
  packed_sequence_size: 0

dataloader:
  _target_: torchdata.stateful_dataloader.StatefulDataLoader
  collate_fn: nemo_automodel.components.datasets.utils.default_collater
  shuffle: True

validation_dataset:
  _target_: nemo_automodel.components.datasets.llm.ChatDataset
  path_or_dataset_id: <<SET>>/sft/validation.jsonl
  seq_length: 16384
  padding: do_not_pad
  truncation: do_not_truncate
  mask_generation_prompt: true

validation_dataloader:
  _target_: torchdata.stateful_dataloader.StatefulDataLoader
  collate_fn: nemo_automodel.components.datasets.utils.default_collater

optimizer:
  _target_: torch.optim.Adam
  betas: [0.9, 0.999]
  eps: 1e-8
  lr: 5.0e-6
  weight_decay: 0

lr_scheduler:
  lr_decay_style: cosine
  min_lr: 5.0e-7

# wandb:
#   project: dark-vessel-triage
#   name: nano-writer-lora-8gpu
